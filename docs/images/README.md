# Screenshot Placeholders

This folder is reserved for README screenshots and demo media.

Recommended files:

- `main-ui.png`
- `graph-example.png`
- `boolean-demo.png`
- `topic-table.png`
- `config-editor.png`
- `demo.gif`

Suggested captures:

- `main-ui.png`: first load of `http://localhost:8080/app/index.html` with the Avi Sinai project header visible.
- `graph-example.png`: `config_files/two_stage_pipeline.conf` deployed in the center graph panel.
- `boolean-demo.png`: `config_files/boolean_gates.conf` deployed with Boolean gate-shaped nodes.
- `topic-table.png`: right panel after publishing values into a config.
- `config-editor.png`: left panel showing inline config, prebuilt agent controls, add-edge editing, and reset.
- `demo.gif`: 20-30 second flow showing deploy, publish, graph update, topic table update, and reset.

The README intentionally references these future assets so the repository has a clear visual documentation checklist.
